import React from "react";
import ReactDOM from "react-dom/client";
import VisionAiPage from "./VisionAiPage";
import "./visionai.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <VisionAiPage />
  </React.StrictMode>
);
